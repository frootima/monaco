configs:
  - id: kafka-reds-rate-metrics
    type:
      api: dashboard
    config:
      name: Kafka REDS - Rate Throughput Metrics
      parameters:
        owner: Monaco Jenkins
      template: dashboard.json
