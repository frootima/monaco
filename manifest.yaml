manifestVersion: "1.0"

projects:
  - name: kafka-reds-observability
    path: projects/kafka-reds-observability

environmentGroups:
  - name: production
    environments:
      - name: ann36102
        url:
          type: environment
          value: DT_ENV_URL
        auth:
          token:
            name: DT_API_TOKEN
          oAuth:
            clientId:
              name: DT_OAUTH_CLIENT_ID
            clientSecret:
              name: DT_OAUTH_CLIENT_SECRET
